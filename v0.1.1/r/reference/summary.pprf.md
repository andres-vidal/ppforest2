# Summary of a pprf forest (shared header + VI).

Summary of a pprf forest (shared header + VI).

## Usage

``` r
# S3 method for class 'pprf'
summary(object, ...)
```

## Arguments

- object:

  A `pprf` model.

- ...:

  Unused.
